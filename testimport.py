#!/usr/bin/python3

from mystuff import sum

first = input("Enter the first value: ")
second = input("Enter the second value: ")

print("The sum of " + first + " and " + second + " is " + str(sum(first,second)))
